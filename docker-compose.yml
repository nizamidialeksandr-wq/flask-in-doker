services:
  web:
    build: .
    ports:
      # слева порт на твоём компьютере, справа порт внутри контейнера
      - "5000:5000"
    volumes:
      # папка проекта видна внутри контейнера:
      # правки в app.py подхватываются без пересборки
      - .:/app
